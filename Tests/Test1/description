Ce test permet de vérifier la commande de base avec juste un fichier log spécifié (sans options) qui contient des requêtes vers tous types de fichiers. Une des adresses est écrites de deux manières (avec ou sans '/' à la fin).
